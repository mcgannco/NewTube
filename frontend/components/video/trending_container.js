import {connect} from 'react-redux';
import Trending from './trending';

const msp = (state) => {
  return({
  })
};

const mdp = dispatch => {
  return({
  })
};

export default connect(msp, mdp)(Trending);
