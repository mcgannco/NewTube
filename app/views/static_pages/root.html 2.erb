<% if logged_in? %>
  <script type="text/javascript">
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  </script>
<% end %>

<script>
  window.logo = "<%= image_path("logo.png") %>"
  window.movie = "<%= asset_path("movie.mp4") %>"
</script>

<div id="root">
</div>
